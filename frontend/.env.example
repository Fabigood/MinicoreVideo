# URL del backend. En local normalmente corre en el puerto 4000.
# En produccion, reemplaza por la URL publica de tu backend desplegado
# (Railway/Render/etc.), por ejemplo: https://mi-backend.up.railway.app/api
VITE_API_URL=http://localhost:4000/api
